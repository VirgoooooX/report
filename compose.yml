services:
  dashboard:
    build: .
    ports:
      - "5050:5050"
    volumes:
      - ./data:/app/data
      - ./runtime:/app/runtime
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5050
      - REPORT_DB_PATH=/app/runtime/report.db
