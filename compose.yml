services:
  dashboard:
    build: .
    ports:
      - "5050:5050"
    volumes:
      - ./rawdata:/app/rawdata
      - ./db:/app/db
    environment:
      - FLASK_HOST=0.0.0.0
      - FLASK_PORT=5050
      - REPORT_RAWDATA_DIR=/app/rawdata
      - REPORT_DB_PATH=/app/db/report.db
